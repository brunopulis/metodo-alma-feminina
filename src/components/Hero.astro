---
import { BookOpen } from "@lucide/astro";
import { Image } from "astro:assets";

import config from "@/data/site-config.json";
import rpmLogo from "@/assets/images/rpm.webp";
---

<section class="bg-red-500 text-white min-h-[90vh] flex items-center">
  <div class="container mx-auto px-4 py-24 md:py-32">
    <div class="flex flex-col md:flex-row items-center max-w-6xl mx-auto">
      <div class="md:w-3/5 md:pr-12">
        <h1
          class="text-4xl md:text-5xl lg:text-6xl font-bold leading-tight mb-8 animate-fade-in"
        >
          Como você ensinaria alguém que não pode te responder com palavras?
        </h1>

        <h2 class="text-xl md:text-2xl leading-relaxed mb-12 text-purple-100">
          Neste livro, você vai descobrir como a comunicação vai muito além da
          fala — e como o RPM pode abrir caminhos reais para a aprendizagem,
          autonomia e expressão de pessoas autistas não falantes.
        </h2>

        <div class="flex flex-col sm:flex-row items-center gap-6">
          <a
            href={config.checkout.url}
            class="bg-white border-1 hover:bg-transparent hover:border-1 hover:text-white text-red-500 font-bold py-4 px-8 rounded-lg text-lg md:text-xl transition-all transform hover:scale-105 bg-transparent duration-300 w-full sm:w-auto"
          >
            Garanta seu exemplar agora
          </a>
        </div>
      </div>

      <div class="md:w-2/5 mt-12 md:mt-0">
        <div class="relative w-full max-w-md mx-auto">
          <div
            class="aspect-[3/4] bg-white rounded-lg shadow-2xl overflow-hidden"
          >
            <div
              class="w-full h-full bg-gradient-to-br from-red-500 to-white flex flex-col"
            >
              <Image
                src={rpmLogo}
                alt=""
                class="w-full object-cover"
                width={348}
                height={533}
              />
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
