---
import { FileText, Download } from "@lucide/astro";
import { Image } from "astro:assets";

import config from "@/data/site-config.json";
import rpmLogo from "@/assets/images/rpm.webp";
---

<section class="py-20 bg-slate-100" id="free-sample">
  <div class="container mx-auto px-4">
    <div class="max-w-5xl mx-auto">
      <div class="bg-white rounded-2xl shadow-lg overflow-hidden">
        <div class="md:flex">
          <div class="md:w-1/2 p-8 md:p-12">
            <div class="flex items-center mb-6">
              <div
                class="w-12 h-12 bg-gray-100 rounded-full flex items-center justify-center mr-4"
              >
                <FileText class="w-6 h-6 text-red-600" />
              </div>
              <h2 class="text-2xl md:text-3xl font-bold text-red-600">
                Amostra Gratuita
              </h2>
            </div>

            <h3 class="text-xl font-semibold mb-4">
              Leia o primeiro capítulo gratuitamente!
            </h3>

            <p class="text-lg text-gray-700 mb-8 leading-relaxed">
              Descubra como o RPM pode mudar sua forma de ensinar, amar e
              acreditar no potencial do seu filho.
            </p>

            <a
              href={config.sample.url}
              target="_blank"
              rel="noopener noreferrer"
              class="inline-flex cursor-pointer items-center bg-red-600 border-1 hover:bg-transparent hover:border-red-600 text-white hover:text-red-600 font-bold py-3 px-6 rounded-lg transition-colors duration-300"
            >
              <Download class="w-5 h-5 mr-2" />
              Leia agora
            </a>
          </div>

          <div
            class="md:w-1/2 bg-gradient-to-br from-red-600 to-red-800 p-8 md:p-12 flex items-center justify-center"
          >
            <div
              class="relative w-64 h-80 bg-white rounded-md shadow-lg transform rotate-3 transition-all duration-300 hover:rotate-0"
            >
              <div class="absolute inset-0 flex flex-col">
                <Image
                  src={rpmLogo}
                  alt=""
                  class="w-full object-cover"
                  width={348}
                  height={533}
                />
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
