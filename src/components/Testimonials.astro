---
import { Quote, Star } from '@lucide/astro';
---

<section class="py-20 bg-gray-50" id="testimonials">
  <div class="container mx-auto px-4">
    <div class="max-w-5xl mx-auto">
      <div class="flex flex-col items-center text-center mb-16">
        <div class="w-16 h-16 rounded-full bg-white flex items-center justify-center mb-6">
          <Quote class="w-8 h-8 text-red-600" />
        </div>
        <h2 class="text-3xl md:text-4xl font-bold text-red-600 mb-4">
          Depoimentos
        </h2>
        <p class="text-lg text-gray-600 max-w-3xl">
          Descubra como o método RPM está transformando vidas e criando novas possibilidades para famílias em todo o Brasil
        </p>
      </div>

      <div class="grid md:grid-cols-2 gap-8 mb-16">
        {[
          {
            quote: "Este livro oferece uma resposta prática para o ensino de pessoas autistas — mesmo aquelas consideradas 'difíceis' ou 'improváveis'.",
            author: "Renata Bonotto",
            stars: 5
          },
          {
            quote: "O RPM mudou a minha visão, a minha postura e a minha esperança com relação ao meu filho. É uma porta sem volta para a transformação.",
            author: "Rosana Calixto de Melo",
            stars: 5
          }
        ].map((testimonial) => (
          <div
            class="bg-white p-8 rounded-xl shadow-md hover:shadow-lg transition-shadow duration-300"
          >
            <div class="flex">
              {[...Array(testimonial.stars)].map((_, i) => (
                <Star key={i} class="w-5 h-5 text-yellow-500 fill-current" />
              ))}
            </div>
            <p class="text-lg my-6 text-gray-700 italic leading-relaxed">
              "{testimonial.quote}"
            </p>
            <div class="flex items-center">
              <p class="font-bold text-gray-800">
                {testimonial.author}
              </p>
            </div>
          </div>
        ))}
      </div>
    </div>
  </div>
</section>
